{% extends 'base.html.twig' %}

{% block title %}Vote{% endblock %}

{% block body %}
    <h1>Vote</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdV</th>
                <td>{{ vote.idV }}</td>
            </tr>
            <tr>
                <th>TypeVote</th>
                <td>{{ vote.typeVote }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vote_index') }}">back to list</a>

    <a href="{{ path('app_vote_edit', {'idV': vote.idV}) }}">edit</a>

    {{ include('vote/_delete_form.html.twig') }}
{% endblock %}
